"""
 markdown frontmatter에 user 를 추가해주세요. 디폴트값은 'ilinkrun@gmail.com'으로 해주세요. 
"""

  ---
  수정 내용

  user: "ilinkrun@gmail.com" 프로퍼티를 모든 기본 템플릿에 추가했습니다.

  ┌───────────────────────────────────────┬────────────────────────────────────────────────────────────┐
  │                 파일                  │                         변경 위치                          │
  ├───────────────────────────────────────┼────────────────────────────────────────────────────────────┤
  │ src/managers/template-manager.ts      │ createDefaultTemplate() — Default 템플릿                   │
  ├───────────────────────────────────────┼────────────────────────────────────────────────────────────┤
  │ src/data/default-ai-chat-templates.ts │ ChatGPT, Claude, Gemini, Google AI Studio, Genspark 템플릿 │
  └───────────────────────────────────────┴────────────────────────────────────────────────────────────┘

  생성되는 frontmatter 예시:
  ---
  title: "..."
  source: "..."
  ...
  tags:
    - "ai-chat"
    - "chatgpt"
  description: "..."
  user: "ilinkrun@gmail.com"
  ---

  주의: 기존에 이미 설치된 확장은 chrome.storage.sync에 저장된 템플릿을 사용합니다. 새 기본값을 적용하려면 설정 페이지(⚙) 
   → 템플릿 선택 → Properties에서 user 항목을 직접 추가하거나, 확장 데이터를 초기화 후 재설치해야 합니다.

==========================

"""
 user는 모든 페이지에 적용되므로  Settings > General 에서 설정하고 템플릿에서는 {{user}}로 불러올 수 있나요? 
"""


  ---
  변경 요약
  
  동작 방식
  - Settings > General 상단에 User 입력 필드 추가 → 입력값이 chrome.storage.sync의 general_settings.user에 저장됨
  - 모든 템플릿에서 {{user}}를 쓰면 저장된 값으로 자동 치환됨

  변경 파일

  ┌──────────────────────────────┬───────────────────────────────────────────────────────────────────┐
  │             파일             │                             변경 내용                             │
  ├──────────────────────────────┼───────────────────────────────────────────────────────────────────┤
  │ storage-utils.ts             │ GeneralSettings에 user 필드 추가, 기본값 'ilinkrun@gmail.com'     │
  ├──────────────────────────────┼───────────────────────────────────────────────────────────────────┤
  │ settings.html                │ General 탭 상단에 User 입력 필드 추가                             │
  ├──────────────────────────────┼───────────────────────────────────────────────────────────────────┤
  │ general-settings.ts          │ initializeUserInput() — 입력값 바인딩 + change 이벤트로 자동 저장 │
  ├──────────────────────────────┼───────────────────────────────────────────────────────────────────┤
  │ content-extractor.ts         │ initializePageContent()의 currentVariables에 '{{user}}' 추가      │
  ├──────────────────────────────┼───────────────────────────────────────────────────────────────────┤
  │ template-manager.ts          │ Default 템플릿 user property 값 → '{{user}}'                      │
  ├──────────────────────────────┼───────────────────────────────────────────────────────────────────┤
  │ default-ai-chat-templates.ts │ AI chat 템플릿 5개 user property 값 → '{{user}}'                  │
  └──────────────────────────────┴───────────────────────────────────────────────────────────────────┘

  기존 설치 사용자: chrome.storage에 저장된 템플릿은 변경되지 않습니다. Settings > 각 템플릿 편집에서 user property의 값을
   {{user}}로 수정해 주세요.
  
